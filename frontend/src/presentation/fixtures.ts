/**
 * Astra specimens are intentionally isolated from presentation state. They may
 * illustrate an unfinished surface, but are never a fallback for Friday data.
 */
export const prototypeFixtureNotice = "Representative Astra specimen — not Friday data.";
